# Production build config for the Titan web app.
# URL of the deployed Cloudflare Worker (see workers/titan-api). Vite inlines
# VITE_* vars at build time; this is what the client calls for AI/food/tickets.
VITE_WORKER_URL=https://titan-api.yoseple23.workers.dev
